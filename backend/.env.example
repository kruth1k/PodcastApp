# Database - for LOCAL development (not docker)
# Default: postgres user with postgres password on localhost
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/podcast_stats

# For DOCKER compose, DATABASE_URL is set automatically via docker-compose.yml
# No need to set it manually when running via docker-compose