---
phase: 03-feed-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/podcasts.py
  - backend/app/services/rss_parser.py
  - web/src/components/PodcastCard.tsx
  - web/src/components/PodcastList.tsx
  - web/src/stores/playerStore.ts
  - web/src/components/EpisodeCard.tsx
  - web/src/lib/api.ts
autonomous: true
requirements:
  - POD-04
  - PLAY-06
  - LIB-02
user_setup: []
must_haves:
  truths:
    - User can manually refresh a podcast feed to see new episodes
    - User can close the app during an episode and return later to continue from the exact same position
    - User can mark an episode as played or unplayed to track their progress
  artifacts:
    - path: "backend/app/routers/podcasts.py"
      provides: "POST /api/podcasts/{id}/refresh and POST /api/podcasts/refresh endpoints"
      exports: ["refresh_podcast", "refresh_all_podcasts"]
    - path: "web/src/stores/playerStore.ts"
      provides: "Position persistence and played status tracking"
      exports: ["loadPosition", "savePosition", "markPlayed", "isPlayed"]
    - path: "web/src/components/PodcastCard.tsx"
      provides: "Refresh button UI on each podcast"
      contains: "onRefresh handler"
    - path: "web/src/components/EpisodeCard.tsx"
      provides: "Played/unplayed visual indicator"
      contains: "played status icon"
  key_links:
    - from: "PodcastCard.tsx"
      to: "/api/podcasts/{id}/refresh"
      via: "api.ts refreshPodcast()"
      pattern: "POST.*refresh"
    - from: "playerStore.ts"
      to: "localStorage"
      via: "localStorage.getItem/setItem"
      pattern: "podcast_position_"
---

<objective>
Implement feed management and playback progress tracking.

Purpose: Users can refresh podcast feeds to get new episodes, resume playback from saved position, and mark episodes as played/unplayed.

Output: Working feed refresh, position persistence, and played status features.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-feed-management/03-CONTEXT.md
@.planning/phases/02-foundation-playback/02-CONTEXT.md
@.planning/ROADMAP.md

# Key interfaces from existing code

From web/src/stores/playerStore.ts:
```typescript
interface Episode {
  id: string;
  podcast_id?: string;
  title: string;
  description: string;
  audio_url: string;
  duration: number | null;
  published_at: string;
  guid: string;
}

interface PlayerState {
  currentEpisode: Episode | null;
  isPlaying: boolean;
  position: number;
  duration: number;
  playbackRate: number;
  playEpisode: (episode: Episode) => void;
  togglePlay: () => void;
  seek: (position: number) => void;
  // ... other methods
}
```

From backend/app/routers/podcasts.py:
- Existing: GET, POST, DELETE endpoints for podcasts
- Need to add: POST refresh endpoints

From web/src/components/PodcastCard.tsx:
- Already has delete button, needs refresh button per D-25
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Feed Refresh Endpoints</name>
  <files>backend/app/routers/podcasts.py</files>
  <action>
Add two new endpoints to podcasts router:

1. POST /{podcast_id}/refresh - Refresh single podcast:
   - Call RSS parser service to re-fetch feed
   - Add new episodes (check by GUID to avoid duplicates)
   - Return updated podcast with new episode count

2. POST /refresh-all - Refresh all subscribed podcasts:
   - Iterate through all podcasts
   - Call RSS parser for each
   - Return summary of refresh results

Reference: backend/app/services/rss_parser.py for parsing logic.
  </action>
  <verify>
<automated>curl -X POST http://localhost:8000/api/podcasts/1/refresh returns 200</automated>
  </verify>
  <done>Both endpoints exist and return valid podcast data with new episodes</done>
</task>

<task type="auto">
  <name>Task 2: Frontend Refresh UI Components</name>
  <files>web/src/components/PodcastCard.tsx, web/src/components/PodcastList.tsx, web/src/lib/api.ts</files>
  <action>
1. Add to api.ts:
   - refreshPodcast(podcastId): POST to /api/podcasts/{id}/refresh
   - refreshAllPodcasts(): POST to /api/podcasts/refresh-all

2. Modify PodcastCard.tsx:
   - Add refresh button (small icon) next to delete button per D-25
   - Show loading spinner while refreshing (D-27)
   - Handle errors: show alert/toast on failure

3. Modify PodcastList.tsx:
   - Add "Refresh All" button in header per D-26
   - Show spinner on button while refreshing any podcast

Per D-25: Per-podcast refresh button on each card
Per D-26: Bulk refresh option in header
Per D-27: Visual feedback during refresh (spinner)
  </action>
  <verify>
<automated>npm run build passes with no errors</automated>
  </verify>
  <done>Refresh buttons visible on each podcast card, bulk refresh in header, loading feedback works</done>
</task>

<task type="auto">
  <name>Task 3: Position Persistence in PlayerStore</name>
  <files>web/src/stores/playerStore.ts</files>
  <action>
Add position persistence methods to playerStore:

1. loadPosition(episodeId): number | null
   - Read from localStorage key: `podcast_position_{episodeId}`
   - Parse JSON: { position: number, updatedAt: number }
   - Return position if exists, null otherwise

2. savePosition(episodeId, position): void
   - Write to localStorage: `podcast_position_{episodeId}`
   - Format: { position, updatedAt: Date.now() }

3. Modify playEpisode(episode):
   - After creating howl, call loadPosition(episode.id)
   - If saved position exists, seek to it with howlInstance.seek()

4. Add position save triggers:
   - On pause (in togglePlay when pausing)
   - On seek (in seek function)
   - On navigate away (useEffect cleanup in PlayerBar)

Per D-28: Store position in localStorage with key `podcast_position_{episodeId}`
Per D-29: Architecture supports both localStorage and backend (defer backend)
  </action>
  <verify>
<automated>npm run build passes</automated>
  </verify>
  <done>Playing an episode resumes from saved position, pausing saves current position</done>
</task>

<task type="auto">
  <name>Task 4: Played/Unplayed Status Tracking</name>
  <files>web/src/stores/playerStore.ts, web/src/components/EpisodeCard.tsx</files>
  <action>
1. Add played status methods to playerStore:
   - isPlayed(episodeId): boolean - check localStorage
   - markPlayed(episodeId): void - set localStorage key
   - markUnplayed(episodeId): void - remove localStorage key
   - Key format: `podcast_played_{episodeId}`

2. Auto-mark on 90% completion:
   - In updatePosition(), check: position / duration > 0.9
   - If true, call markPlayed(episode.id)

3. Modify EpisodeCard.tsx:
   - Import isPlayed from playerStore or create hook
   - Show visual indicator per D-31:
     - Played: grayed text, checkmark icon
     - Unplayed: bold text, dot indicator
   - Add click handler to toggle played status manually per D-32

Per D-30: Auto-mark on 90% completion
Per D-31: Visual indicator (played = grayed + checkmark, unplayed = bold + dot)
Per D-32: Manual toggle option
  </action>
  <verify>
<automated>npm run build passes</automated>
  </verify>
  <done>Episodes show played/unplayed status, auto-mark at 90%, manual toggle works</done>
</task>

</tasks>

<verification>
- [ ] Backend refresh endpoints return new episodes
- [ ] Frontend refresh buttons work with visual feedback
- [ ] Position saves on pause/seek, loads on play
- [ ] Episodes auto-mark as played at 90%
- [ ] Played/unplayed indicators display correctly
- [ ] Manual toggle of played status works
</verification>

<success_criteria>
1. User can manually refresh a podcast feed to see new episodes
2. User can close the app during an episode and return later to continue from the exact same position
3. User can mark an episode as played or unplayed to track their progress
</success_criteria>

<output>
After completion, create `.planning/phases/03-feed-management/03-01-SUMMARY.md`
</output>