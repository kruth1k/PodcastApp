---
phase: "05-basic-statistics"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "web/src/app/stats/page.tsx"
  - "web/src/stores/statsStore.ts"
autonomous: true
requirements:
  - "STAT-02"
  - "STAT-03"
  - "STAT-04"

must_haves:
  truths:
    - "User can view total minutes listened on statistics dashboard"
    - "User can see ranked list of most listened podcasts"
    - "User can see ranked list of most listened episodes"
    - "User can filter statistics by time period (last 7 days, 30 days, year, all time)"
  artifacts:
    - path: "web/src/app/stats/page.tsx"
      provides: "Statistics dashboard with total time, podcast rankings, episode rankings"
      min_lines: 100
    - path: "web/src/stores/statsStore.ts"
      provides: "getTotalListeningTime, getPodcastStats, getEpisodeStats with time filtering"
      exports: ["getTotalListeningTime", "getPodcastStats", "getEpisodeStats", "getFilteredStats"]
  key_links:
    - from: "web/src/app/stats/page.tsx"
      to: "web/src/stores/statsStore.ts"
      via: "useStatsStore hook"
      pattern: "useStatsStore.*get.*Stats"
---

<objective>
Implement basic statistics display with time period filtering.

Purpose: Allow users to view their listening statistics (total time, top podcasts, top episodes) with filtering by time period.
Output: Working statistics dashboard with time filter controls.
</objective>

<context>
@.planning/ROADMAP.md (Phase 5 requirements)
@.planning/STATE.md (Current position)
@.planning/phases/04-01/04-01-SUMMARY.md (Phase 4 completion - statsStore created)

Existing codebase:
- web/src/stores/statsStore.ts - already has getTotalListeningTime(), getPodcastStats(), getEpisodeStats()
- web/src/app/stats/page.tsx - existing stats page with display UI
- web/src/stores/playerStore.ts - event capture integrated in Phase 4

Interface requirements from statsStore:
- getTotalListeningTime(): number - returns total seconds
- getPodcastStats(): PodcastStats[] - returns [{podcast_id, total_seconds, session_count}]
- getEpisodeStats(): EpisodeStats[] - returns [{episode_id, total_seconds, session_count}]
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add time filtering to statsStore</name>
  <files>web/src/stores/statsStore.ts</files>
  <action>
Add time period filtering capability to statsStore with the following methods:
- getFilteredStats(timePeriod: '7days' | '30days' | 'year' | 'all') that returns filtered versions of getTotalListeningTime, getPodcastStats, getEpisodeStats
- Filter logic: calculate start date based on period, filter dailyStats/events by date range before aggregating
- Add getPodcastStatsByPeriod(period) and getEpisodeStatsByPeriod(period) methods
- Export TimePeriod type for use in components
  </action>
  <verify>
<automated>TypeScript compiles without errors: cd web && npx tsc --noEmit</automated>
  </verify>
  <done>statsStore has time filtering methods, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Add time period filter UI to stats page</name>
  <files>web/src/app/stats/page.tsx</files>
  <action>
Update the stats page to include time period filtering:
- Add a dropdown/segmented control with options: "Last 7 days", "Last 30 days", "This year", "All time"
- Use state to track selected time period
- Pass time period to statsStore methods to get filtered results
- Display total listening time, top podcasts, and top episodes based on selected period
- Style the filter controls using existing UI patterns (Tailwind classes)
  </action>
  <verify>
<automated>TypeScript compiles without errors: cd web && npx tsc --noEmit</automated>
  </verify>
  <done>Stats page has working time period filter, displays filtered statistics</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Complete statistics dashboard with time filtering</what-built>
  <how-to-verify>
    1. Start the app: cd web && npm run dev
    2. Navigate to http://localhost:3000/stats
    3. Verify you can see total listening time
    4. Verify you can see ranked list of podcasts
    5. Verify you can see ranked list of episodes
    6. Change time period filter and verify statistics update accordingly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] statsStore has time filtering methods
- [ ] Stats page renders total listening time
- [ ] Stats page renders top podcasts list
- [ ] Stats page renders top episodes list
- [ ] Time period filter changes the displayed statistics
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
Statistics dashboard displays:
- Total listening time (STAT-02)
- Top podcasts ranked by time (STAT-03)
- Top episodes ranked by time (STAT-04)
- Time period filtering (last 7 days, 30 days, year, all time)
</success_criteria>

<output>
After completion, create `.planning/phases/05-basic-statistics/05-01-SUMMARY.md`
</output>