---
phase: 04-event-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useVisibilityChange.ts
  - src/hooks/useMediaSession.ts
  - src/components/BackgroundMiniPlayer.tsx
  - src/stores/statsStore.ts
  - src/stores/playerStore.ts
autonomous: true
requirements:
  - PLAY-07
  - STAT-01

must_haves:
  truths:
    - "Audio continues playing when user minimizes or switches away from the app"
    - "System accurately tracks minutes watched for each episode session"
    - "User can control playback from a system notification when app is backgrounded"
  artifacts:
    - path: "src/hooks/useVisibilityChange.ts"
      provides: "Page Visibility API hook"
      min_lines: 20
    - path: "src/hooks/useMediaSession.ts"
      provides: "Media Session API integration"
      min_lines: 30
    - path: "src/components/BackgroundMiniPlayer.tsx"
      provides: "Notification-style mini player when backgrounded"
      min_lines: 40
    - path: "src/stores/statsStore.ts"
      provides: "ListeningEvent storage and session tracking"
      min_lines: 60
  key_links:
    - from: "BackgroundMiniPlayer"
      to: "playerStore"
      via: "usePlayerStore() for current episode and play state"
    - from: "playerStore (onplay callback)"
      to: "statsStore"
      via: "captureEvent() call on playback events"
    - from: "useVisibilityChange"
      to: "BackgroundMiniPlayer"
      via: "onVisibilityChange callback"
---

<objective>
**Goal:** Implement background playback and event tracking for statistics

**Purpose:** Phase 4 establishes the event capture infrastructure that powers all future statistics (Phase 5-6), while enabling playback to continue when users minimize or switch tabs.

**Output:** Working background playback with event capture
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-event-tracking/04-CONTEXT.md
@.planning/phases/02-foundation-playback/02-CONTEXT.md

# Key interfaces from existing code:

From src/stores/playerStore.ts (Phase 2):
- interface PlayerStore { currentEpisode, isPlaying, position, duration, playbackRate, togglePlay, seek, skipForward, skipBackward }
- Episode interface has: id, title, audio_url, podcast_id
- Podcast interface has: id, title, image_url
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Page Visibility and Media Session hooks</name>
  <files>src/hooks/useVisibilityChange.ts, src/hooks/useMediaSession.ts</files>
  <action>
    Create two hooks in src/hooks/:

    1. **useVisibilityChange.ts**: Export function useVisibilityChange(callback: (hidden: boolean) => void) that:
       - Adds visibilitychange event listener to document
       - Calls callback with document.hidden on visibility change
       - Cleans up on unmount
       - Memoize callback with useCallback

    2. **useMediaSession.ts**: Export function useMediaSession(episode, podcast, isPlaying) that:
       - Checks if 'mediaSession' exists in navigator (guard)
       - Sets MediaMetadata with title, artist, artwork on episode/podcast change
       - Registers action handlers for: play, pause, seekbackward (30s), seekforward (15s)
       - Each handler calls the respective playerStore methods (import and use directly)
       - Update metadata when episode or isPlaying changes

    Per D-33 (web): Use Page Visibility API + Media Session API
  </action>
  <verify>
    <automated>tsc --noEmit src/hooks/useVisibilityChange.ts src/hooks/useMediaSession.ts 2>&1 | head -20</automated>
  </verify>
  <done>Both hooks compile without errors, exports are functions</done>
</task>

<task type="auto">
  <name>Task 2: Create BackgroundMiniPlayer component</name>
  <files>src/components/BackgroundMiniPlayer.tsx</files>
  <action>
    Create src/components/BackgroundMiniPlayer.tsx:

    - Use useVisibilityChange hook to detect when page becomes hidden
    - Use usePlayerStore to get currentEpisode, isPlaying, togglePlay, skipBackward, skipForward
    - Use usePodcastStore to get podcast by currentEpisode.podcast_id (need getPodcast method)
    - Render floating notification-style player ONLY when document.hidden === true and currentEpisode exists
    - Layout: Fixed position top-right corner, z-index: 9999, min-width: 320px, background: dark with blur
    - Show: Episode title, podcast name, skip backward, play/pause, skip forward, close button
    - Close button: Calls togglePlay (stops playback) and dismisses mini player (use local state)
    - Style: Dark background (#1a1a1a), rounded corners, subtle shadow, white text

    Per D-34: Notification-style mini player shown when tab becomes hidden
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -i "BackgroundMiniPlayer" | head -5</automated>
  </verify>
  <done>Component renders without errors, shows when tab hidden, hides when visible</done>
</task>

<task type="auto">
  <name>Task 3: Create statsStore with event capture and integrate with playerStore</name>
  <files>src/stores/statsStore.ts, src/stores/playerStore.ts</files>
  <action>
    Create src/stores/statsStore.ts with ListeningEvent interface and event capture:

    1. Define ListeningEvent interface (per D-37):
       - id: string (UUID), episode_id: string, podcast_id: string
       - event_type: 'play' | 'pause' | 'seek_forward' | 'seek_back' | 'complete' | 'progress'
       - timestamp: number (Unix ms), position_seconds: number, previous_position?: number
       - playback_rate: number, session_id: string

    2. Define DailyStats interface (per D-39):
       - date: string (YYYY-MM-DD), podcast_id: string
       - total_seconds: number, session_count: number

    3. Create statsStore with:
       - events: ListeningEvent[] (persisted to localStorage 'podcast_stats_events')
       - dailyStats: DailyStats[] (persisted to localStorage 'podcast_stats_daily')
       - currentSessionId: string | null
       - lastProgressTime: number (for deduplication per D-40)
       - captureEvent(event_type, position, previous_position?) method
       - aggregateDailyStats() method to compute from events
       - Session management: start new session on 'play', end on 'pause' > 30s or 'complete'

    4. Add event capture callbacks to src/stores/playerStore.ts:
       - onplay: capture 'play', create new session_id, start 30s progress heartbeat
       - onpause: capture 'pause', check if pause > 30s ends session
       - onseek: capture 'seek_forward' or 'seek_back' with previous_position
       - onend: capture 'complete', end session
       - Progress: capture 'progress' every 30 seconds (skip if < 25s since last per D-40)

    Per D-36: Event capture integrated into playerStore callbacks
    Per D-38: Session-based tracking with heartbeat
    Per D-40: Event deduplication (skip progress if < 25s) and validation
  </action>
  <verify>
    <automated>tsc --noEmit src/stores/statsStore.ts 2>&1 | head -10</automated>
  </verify>
  <done>statsStore created, playerStore modified with event capture, all compilation passes</done>
</task>

</tasks>

<verification>
[ ] BackgroundMiniPlayer appears when tab becomes hidden
[ ] Media controls in system notification work (play/pause/skip)
[ ] ListeningEvent captured on play, pause, seek, progress events
[ ] Session tracking correctly starts/ends sessions
[ ] Events persisted to localStorage
[ ] Progress events limited to every 25+ seconds (deduplication)
</verification>

<success_criteria>
[ ] Audio continues playing when app minimized (howler.js with html5: true)
[ ] BackgroundMiniPlayer appears when tab hidden with episode info and controls
[ ] System notification shows Media Session controls that work
[ ] ListeningEvent captured for every play/pause/seek/complete/progress
[ ] Session-based tracking with 30s heartbeat computes actual listening time
[ ] Events stored in localStorage for statistics phases
</success_criteria>

<output>
After completion, create .planning/phases/04-event-tracking/04-01-SUMMARY.md
</output>