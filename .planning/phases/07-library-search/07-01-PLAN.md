---
phase: 07-library-search
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/stores/podcastStore.ts
  - web/src/components/SearchResults.tsx
  - web/src/app/page.tsx
autonomous: true
requirements:
  - LIB-04
must_haves:
  truths:
    - User can search for episodes by title and see results appear as they type
    - User can click a search result and go directly to that episode
    - Search works across all subscribed podcasts simultaneously
  artifacts:
    - path: "web/src/stores/podcastStore.ts"
      provides: "Search filtering methods"
      contains: "searchEpisodes, searchPodcasts"
    - path: "web/src/components/SearchResults.tsx"
      provides: "Search dropdown UI component"
      exports: "SearchResults"
    - path: "web/src/app/page.tsx"
      provides: "Home page integration"
      contains: "searchQuery, searchResults, debounced search"
  key_links:
    - from: "web/src/app/page.tsx"
      to: "web/src/stores/podcastStore.ts"
      via: "searchEpisodes() call"
      pattern: "searchEpisodes.*searchQuery"
    - from: "web/src/components/SearchResults.tsx"
      to: "web/src/stores/podcastStore.ts"
      via: "selectPodcast() to navigate to episode"
      pattern: "onSelect.*selectPodcast"
---

<objective>
Implement library search functionality - users can search episodes across all subscribed podcasts with live results as they type.

Purpose: Fulfills LIB-04 requirement - user can search for episodes within their library with instant results
Output: Working search on home page with dropdown results
</objective>

<context>
@.planning/phases/07-library-search/07-CONTEXT.md
@.planning/phases/06-time-grouped-stats/06-01-SUMMARY.md
@web/src/stores/podcastStore.ts
@web/src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add search methods to podcastStore</name>
  <files>web/src/stores/podcastStore.ts</files>
  <action>
Add two search methods to the podcastStore:

1. `searchPodcasts(query: string): Podcast[]` - Filters podcasts array by title (case-insensitive, partial match)
2. `searchEpisodes(query: string): Episode[]` - Searches ALL episodes across ALL podcasts, returns episodes with podcast info attached (so UI knows which podcast each episode belongs to)

Both methods should:
- Trim whitespace and ignore empty queries
- Use toLowerCase() for case-insensitive comparison
- Use includes() for partial matching
- Return empty array if query length < 2

Export these as part of the store interface and implementation.
  </action>
  <verify>
<automated>grep -n "searchPodcasts\|searchEpisodes" web/src/stores/podcastStore.ts</automated>
  </verify>
  <done>podcastStore has searchPodcasts and searchEpisodes methods that return matching items</done>
</task>

<task type="auto">
  <name>Task 2: Create SearchResults dropdown component</name>
  <files>web/src/components/SearchResults.tsx</files>
  <action>
Create a new SearchResults component:

```tsx
interface SearchResult {
  id: string;
  title: string;
  podcastTitle: string;
  episodeId: string;
  podcastId: string;
}

interface SearchResultsProps {
  results: SearchResult[];
  onSelect: (podcastId: string, episodeId: string) => void;
  isOpen: boolean;
}
```

- Render results as a dropdown list below search input
- Each result shows: episode title (bold), podcast name below
- Max 10 results displayed
- Clicking a result calls onSelect with podcastId and episodeId
- Show "No results found" when results empty but search active
- Only render when isOpen and results.length > 0 (or showing empty message)
- Use absolute positioning, z-index above other content
- Add subtle shadow, rounded corners, max-height with scroll for overflow

The component should be reusable but specifically designed for the home page search dropdown.
  </action>
  <verify>
<automated>ls web/src/components/SearchResults.tsx</automated>
  </verify>
  <done>SearchResults.tsx component exists with proper props interface and renders dropdown</done>
</task>

<task type="auto">
  <name>Task 3: Integrate search into home page</name>
  <files>web/src/app/page.tsx</files>
  <action>
Update page.tsx to add search functionality:

1. Add state: searchQuery (string), searchResults (array), showResults (boolean)
2. Import SearchResults component
3. Add search input field ABOVE the existing "Add Podcast" form (per D-50)
4. Implement debounced search (300ms delay):
   - On search input change, set searchQuery
   - useEffect watches searchQuery, calls searchEpisodes() after debounce
   - Store results in searchResults state
   - Set showResults = true when results exist
5. Render SearchResults dropdown below search input
6. onSelect handler: calls selectPodcast() with the podcast, showing its episodes in the right panel
7. Close dropdown when: user clicks outside, presses Escape, or clears search

Search behavior per D-51, D-52, D-53:
- Case insensitive, partial matching
- Live results as user types
- Up to 10 results in dropdown

Keep existing "Add Podcast" functionality - search is an ADDITIONAL input above it.
  </action>
  <verify>
<parameter name="automated">grep -n "searchQuery\|SearchResults" web/src/app/page.tsx